# Terminal 1 - Query Server
python -m servers.query_server.main

# Terminal 2 - Transactional Server
python -m servers.transactional_server.main

# Terminal 3 - External API Server
python -m servers.external_api_server.main

# Terminal 4 - Agent Host
python -m agent_host.main